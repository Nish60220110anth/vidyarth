model summary {
  id         Int     @id @default(autoincrement())
  companyId  Int     @unique
  summary    String  @db.MediumText
  is_changed Boolean @default(false)
  company    company @relation("SummaryCompany", fields: [companyId], references: [id], onDelete: Cascade)
}
