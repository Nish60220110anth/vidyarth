model summary {
  id Int @id @default(autoincrement())

  companyId Int @unique
  company   company @relation("SummaryCompany", fields: [companyId], references: [id])

  summary String @db.MediumText

  is_changed Boolean @default(false)
}
